/** Category label → first account type card (px). */
export const ACCOUNT_TYPE_PICKER_CATEGORY_TO_FIRST_GAP_PX = 8;

/** Account type card → account type card (px). */
export const ACCOUNT_TYPE_PICKER_CARD_GAP_PX = 8;

/** Last card in category → next category label (px). */
export const ACCOUNT_TYPE_PICKER_CATEGORY_GAP_PX = 16;

/** Icon frame edge length (px) — matches HeaderIconButton `bg-muted` surface. */
export const ACCOUNT_TYPE_PICKER_ICON_FRAME_SIZE_PX = 40;

/** Account type icon size inside the frame (px). */
export const ACCOUNT_TYPE_PICKER_ICON_SIZE_PX = 24;
