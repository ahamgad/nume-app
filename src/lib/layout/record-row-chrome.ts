/** Record title — matches account details interest-detail label typography. */
export const RECORD_ROW_LABEL_CLASS =
  "truncate text-[0.9375rem] leading-snug text-muted-foreground";

/** Record amount — same size as title, medium weight. */
export const RECORD_ROW_AMOUNT_CLASS = "text-[0.9375rem] font-medium";

/** Record meta — matches account details header metadata (type · number). */
export const RECORD_ROW_META_CLASS =
  "mt-0.5 truncate text-[0.8125rem] leading-snug text-muted-foreground tabular-nums";
