/**
 * Prototype-only flag. Keep false in production — orbit splash remains default.
 * Set true locally to evaluate the stroke-reveal splash concept.
 */
export const SPLASH_USE_STROKE_PROTOTYPE = false;
