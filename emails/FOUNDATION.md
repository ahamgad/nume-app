# Superseded draft

This document is not active.

The OTP email is being redesigned from scratch in `emails/otp/template.html` and `src/lib/email/otp-email-template.ts`.

Once approved, that template will become the new Email Foundation.
